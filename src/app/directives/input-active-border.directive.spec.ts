import { InputActiveBorderDirective } from './input-active-border.directive';

describe('InputActiveBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputActiveBorderDirective();
    expect(directive).toBeTruthy();
  });
});
