import { InputChangeWidthDirective } from './input-change-width.directive';

describe('InputChangeWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new InputChangeWidthDirective();
    expect(directive).toBeTruthy();
  });
});
